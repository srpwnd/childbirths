# childbirths
### Requirements
You need to have installed `docker` and `docker-compose`.
### How to run
1. clone this repo
2. navigate to the root directory of cloned repo
3. run this command:

    `docker-compose up`